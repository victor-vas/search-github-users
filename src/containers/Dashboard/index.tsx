import { Info, User } from '../../components';
import Repos from '../../components/Repos';

const Dashboard = () => {
  return (
    <main>
      <Info />
      <User />
      <Repos />
    </main>
  );
};

export default Dashboard;
